import './App.css';

function App() {
  return (
    <div className="App">
      <header className="App-header">
        <p>Party List</p>
	<p>* TODO</p>
	<p>TODO if (roles.includes "host") { "Create New Party" }</p>
      </header>
    </div>
  );
}

export default App;
