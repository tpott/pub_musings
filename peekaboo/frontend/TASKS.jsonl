{"id": 150, "description": "Fix PeekabooFlow.destroy() to clean up HTTP-mode AudioRecorder and media audio", "status": "done", "done_when": "1) AudioRecorder has public destroy() method that stops recording and releases stream/resources 2) PeekabooFlow.destroy() calls recorder.destroy() to clean up HTTP-mode mic stream 3) PeekabooFlow.destroy() calls display.stopAudio() to stop any playing media audio 4) Unit tests for AudioRecorder.destroy() (stops recording, handles not-recording) 5) Unit tests for PeekabooFlow.destroy() (calls recorder.destroy, calls display.stopAudio) 6) All mock setups updated with destroy method 7) npm test passes"}
