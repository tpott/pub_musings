{"id": 235, "description": "Implement ResendEmailSender for production email delivery", "status": "done", "done_when": "All 9 criteria met (manual Resend test requires RESEND_API_KEY)"}
{"id": 238, "description": "Update .env.example and DEPLOY.md with email configuration", "status": "done", "done_when": "All 3 criteria met"}
{"id": 239, "description": "Add account settings page with TOTP management", "status": "done", "done_when": "Criteria 1-4 and 7 met. Tests deferred to task 240."}
{"id": 240, "description": "Add E2E tests for settings page and TOTP flows", "status": "done", "done_when": "All 4 criteria met: 11 E2E tests in peekaboo-settings.spec.ts, all pass, settings button visibility tested"}
{"id": 241, "description": "Add ProcessTranscript tests for Anthropic provider", "status": "done", "done_when": "13 tests in anthropic_transcript_test.go (491 lines): show_media success, TTS+show_media, multiple show_media enforcement, wait_for_more, TTS only, API errors (400/429/500/503), malformed JSON, invalid tool inputs (3 tools), context cancellation, network error, no tool_use blocks, request structure, error body truncation. All pass."}
{"id": 242, "description": "Add ProcessTranscript tests for OpenAI provider", "status": "done", "done_when": "15 tests in openai_transcript_test.go (494 lines): show_media success, TTS+show_media, multiple show_media enforcement, wait_for_more, TTS only, API errors (400/429/500/503), malformed JSON, empty choices, invalid function args (3 tools), context cancellation, network error, non-function tool call, request structure, error body truncation. All pass."}
{"id": 243, "description": "Add login button on main page for logged-out users (analogous to logout/settings buttons)", "status": "done", "done_when": "LoginButton.astro created with login icon, positioned top-left, hidden when authenticated, visible when logged out. Added to index.astro. 2 E2E tests (visible/hidden) pass."}
{"id": 244, "description": "Replace skip-to-content link on /login with a styled back button", "status": "done", "done_when": "Login and register pages have styled back buttons (circular, arrow icon) replacing unstyled skip-to-content links. Back buttons link to / and match LogoutButton/SettingsButton styling. All 21 auth E2E tests pass."}
{"id": 245, "description": "Remove Welcome back TTS greeting on microphone click", "status": "done", "done_when": "Removed greeting code from websocket.go (greetedSessions map, greetedMu mutex, executeTTS call). Deleted websocket_tts_greeting_test.go. All backend tests pass."}
{"id": 246, "description": "Remove TTS narration for media display - just play media audio directly", "status": "done", "done_when": "Added explicit rule to LLM system prompt: 'Do NOT call text_to_speech when calling show_media for a single recognized subject.' Updated tool description in both providers. All LLM tests pass."}
{"id": 247, "description": "Investigate and fix 'Service temporarily unavailable' errors", "status": "done", "done_when": "Root cause: WebSocket error handler always set type='server', which mapped to generic message. Fix: changed to type='client' so backend error messages (transcription failed, etc.) pass through to user. LEARNINGS.md entry added."}
{"id": 248, "description": "Reduce 'No speech detected' error spam", "status": "done", "done_when": "Backend silently ignores empty transcripts instead of sending error. Frontend HTTP path silently resets to idle. Tests updated in both backend and frontend."}
{"id": 249, "description": "Close WebSocket when microphone is tapped off", "status": "done", "done_when": "stopWebSocketRecording() now calls wsClient.disconnect() after stopRecording(). WebSocket closes when mic tapped off, returns to idle. Next recording tap reconnects. Tests updated."}
{"id": 250, "description": "Switch audio transport from raw binary to base64-encoded JSON messages", "status": "done", "done_when": "Frontend sendAudioChunk sends JSON {type:audio_data, data:<base64>, seq, client_time}. Backend handleAudioDataMessage decodes base64 and buffers audio. Legacy binary path preserved for backward compat. 3 new backend tests + 5 updated frontend tests pass."}
{"id": 251, "description": "Fix fetch-feedback.py cursor behavior - always update .feedback-cursor", "status": "done", "done_when": "Cursor advances past filtered-out items using all_newest_ts computed before trust filtering. When all items filtered, cursor still updates. max(newest_ts, all_newest_ts) used for written items. No re-fetch of already-seen items."}
{"id": 252, "description": "Add user_id to feedback records and admin API response", "status": "done", "done_when": "Feedback struct has UserID *string. Schema has user_id column + ALTER TABLE migration. FeedbackHandler extracts user_id from session. Admin API includes user_id in response. 2 new DB tests + all existing tests pass."}
{"id": 253, "description": "Add io.LimitReader to JSON decode success paths for external API responses", "status": "done", "done_when": "4 success-path JSON decodes now use io.ReadAll(io.LimitReader(resp.Body, maxSuccessBodyBytes)): transcribe.go (whisper HTTP), websocket_audio.go (whisper WS), openai.go (ExtractIntent), anthropic.go (ExtractIntent). maxSuccessBodyBytes=1MB defined in api/ and llm/ packages. All backend tests pass."}
{"id": 254, "description": "Add rate limiting to auth login and register endpoints", "status": "done", "done_when": "POST /api/auth/login wrapped with loginLimiter (10 req/min). POST /api/auth/register wrapped with registerLimiter (5 req/min). All backend tests pass."}
{"id": 255, "description": "Add rate limiting to TOTP and remaining auth endpoints", "status": "done", "done_when": "TOTP setup/enable/disable wrapped with totpLimiter (10 req/min). CSRF endpoint wrapped with csrfLimiter (30 req/min). All backend tests pass."}
{"id": 256, "description": "Fix WebSocket E2E tests broken by task 249 disconnect and task 250 base64 audio", "status": "done", "done_when": "Two root causes fixed: (1) stopWebSocketRecording() no longer disconnects immediately — waits for server response, disconnects after handleWsMedia when recorder inactive. (2) E2E mock WebSocket handlers updated to recognize audio_data JSON messages instead of binary. All 9 previously-failing E2E tests pass. 324 unit tests + 55 E2E tests pass."}
{"id": 257, "description": "Add X-CSRF-Token to CORS allowed headers", "status": "done", "done_when": "cors.go Access-Control-Allow-Headers includes X-CSRF-Token. cors_test.go updated. API.md CORS section updated. All backend tests pass."}
{"id": 258, "description": "Extract duplicated checkAuth() into shared auth utility", "status": "done", "done_when": "checkAuth() added to frontend/src/lib/auth.ts. LoginButton, LogoutButton, SettingsButton import from auth.ts. FeedbackButton doesn't use auth check (unchanged). Build, 324 unit tests, 55 E2E tests pass."}
{"id": 259, "description": "Update API.md with auth endpoint rate limits", "status": "done", "done_when": "API.md endpoint table updated: login 10/min+lockout, register 5/min, TOTP 10/min, CSRF 30/min. Rate Limiting section updated with all auth endpoints."}
{"id": 260, "description": "Update DEPLOY.md environment variables section", "status": "done", "done_when": "DEPLOY.md references .env.example and lists 8 key production variables (PORT, WHISPER, ANTHROPIC, LLM_PROVIDER, ALLOWED_ORIGIN, CSRF_SECRET, HTTPS_ONLY, TRUSTED_USERS)."}
{"id": 261, "description": "Update architecture.md phase status (VAD is implemented)", "status": "done", "done_when": "Phase 2 annotation updated (auth done). Phase 3 VAD item struck through with task 143 reference. Phase 3 header annotated (VAD done)."}
{"id": 262, "description": "Add admin handler fail-closed test (empty TRUSTED_USERS)", "status": "done", "done_when": "Test already existed (TestAdminFeedback_NoTrustedUsers). Enhanced with error message assertion: verifies 403 AND 'TRUSTED_USERS not configured' error. All backend tests pass."}
{"id": 263, "description": "Fix ALTER TABLE migration to only ignore 'duplicate column' errors", "status": "done", "done_when": "db.go migration checks error string for 'duplicate column' and returns non-nil for other errors. All backend tests pass."}
{"id": 264, "description": "Fix WebSocket idle timeout to skip check during active processing", "status": "done", "done_when": "idleTimeoutWatcher checks state.isProcessing and skips timeout when true. All backend tests pass (101s). No unit test added — internal goroutine requires integration test infrastructure."}
{"id": 265, "description": "Clarify admin authorization logic ordering in handlers_admin.go", "status": "done", "done_when": "Reordered: empty check first (fail-closed), then membership check without compound condition. Admin tests pass."}
{"id": 266, "description": "Fix settings page to distinguish auth vs network errors", "status": "done", "done_when": "loadUserInfo() only redirects to /login on 401/403. Shows retry-friendly error message with retry button for network failures. 11 E2E settings tests pass. Build passes."}
{"id": 267, "description": "Add theme variables for box-shadow and success message colors", "status": "done", "done_when": "theme.css has --shadow-card, --success-bg, --success-border for both light/dark. All 5 auth pages + settings use theme vars instead of hardcoded rgba. Dark mode override removed from settings. 35 E2E tests pass."}
{"id": 268, "description": "Update specs/auth.md to remove obsolete SMTP env vars", "status": "done", "done_when": "Replaced 4 SMTP vars with RESEND_API_KEY, APP_URL, updated EMAIL_FROM default."}
{"id": 269, "description": "Add TOTP/2FA section to DEPLOY.md", "status": "done", "done_when": "DEPLOY.md has TOTP section with setup flow, endpoint table, and notes (magic links bypass TOTP, ±1 period skew, rate limits)."}
{"id": 270, "description": "Use net.SplitHostPort in getClientIP for correct IPv6 handling", "status": "done", "done_when": "getClientIP() uses net.SplitHostPort() instead of manual port stripping. Test cases added for IPv6 with port ([::1]:8080), IPv6 without port (::1), and IPv4 (unchanged). All backend tests pass."}
{"id": 271, "description": "Wrap localStorage calls in try-catch for private browsing compatibility", "status": "done", "done_when": "ThemeInit.astro, ThemeToggle.astro, and feedback.ts localStorage calls wrapped in try-catch with sensible fallbacks (default theme, generated session ID). Build passes, existing tests pass."}
{"id": 272, "description": "Add password length validation in settings page TOTP flows", "status": "done", "done_when": "Settings page TOTP setup and disable forms use validatePassword() from auth.ts (>=8 chars). Build passes, 11 E2E settings tests pass."}
{"id": 273, "description": "Optimize base64 encoding of audio chunks to avoid string concatenation", "status": "done", "done_when": "sendAudioChunk uses chunked String.fromCharCode.apply (8KB chunks) instead of per-byte concatenation. Build + 324 unit tests pass."}
{"id": 274, "description": "Clean up TTS blob URLs on PeekabooFlow destroy", "status": "done", "done_when": "text-to-speech.ts tracks active blob URLs in Set, cleanupTTSBlobUrls() revokes all. PeekabooFlow.destroy() calls it. 3 test mock files updated. Build + 324 tests pass."}
{"id": 275, "description": "Add interaction logging and POST /api/log to API.md", "status": "done", "done_when": "API.md documents POST /api/log (dev-only), interaction logging env vars, key interaction fields. Also updated WebSocket protocol to show base64 JSON audio_data (preferred) and legacy binary."}
{"id": 276, "description": "Remove skip-to-content links from all pages", "status": "done", "done_when": "Removed skip-to-content anchor + CSS from index.astro. Removed id=main-content tabindex=-1 from main tags on all 6 pages (index, settings, magic-link, verify-email, login, register). Build + E2E pass."}
{"id": 277, "description": "Add logout confirmation dialog", "status": "done", "done_when": "LogoutButton.astro click handler shows confirm() before logging out. E2E peekaboo-auth.spec.ts updated with dialog.accept() handler. 55 E2E tests pass."}
{"id": 278, "description": "Redirect logged-in users away from /login page", "status": "done", "done_when": "login.astro calls checkAuth() on load, redirects to / if authenticated. Existing E2E tests unaffected (mock doesn't respond to /api/auth/me so checkAuth returns false)."}
{"id": 279, "description": "Investigate media 404 error report", "status": "done", "done_when": "Could not reproduce. Verified: media files exist at data/media/cat/set1/photo.jpg, EncryptedFileServer path resolution correct, seedMediaFromDisk scans correct directory, peekaboo.service WorkingDirectory matches. Likely transient production issue."}
{"id": 280, "description": "Suppress TTS narration when show_media is present in LLM response", "status": "done", "done_when": "Added dropTTSWithShowMedia() in provider.go. Called from parseToolActions (anthropic.go) and parseOpenAIToolActions (openai.go). Tests updated: both LLM transcript tests expect TTS dropped, WebSocket TTS test verifies no tts_audio sent with show_media. All backend + frontend + E2E tests pass."}
{"id": 281, "description": "Propagate context to transcribeAudio for cancellation on WebSocket disconnect", "status": "done", "done_when": "transcribeAudio and forwardToWhisper both accept context.Context. Both use http.NewRequestWithContext. processAudio passes ctx, ServeHTTP passes r.Context(). All backend tests pass."}
{"id": 282, "description": "Add logger.debug to silent catch blocks in frontend", "status": "done", "done_when": "peekaboo-flow.ts audio.play().catch and media-display.ts audio.play().catch log at debug level. feedback.ts Math.random UUID fallback replaced with crypto.getRandomValues. Build + 324 unit tests + 55 E2E tests pass."}
{"id": 283, "description": "Add max-length validation for auth token query parameters", "status": "done", "done_when": "maxTokenLength=128 const added. HandleVerify and HandleMagicLinkVerify reject tokens > 128 chars with 400 'invalid token'. TestVerify_OversizedToken and TestMagicLinkVerify_OversizedToken added. All backend tests + lint pass."}
{"id": 284, "description": "Add transcript length validation to WebSocket processTranscript path", "status": "done", "done_when": "maxTranscriptLength=500 const in websocket_interaction.go. processTranscript rejects transcripts >500 chars. TestAudioWebSocketHandler_OversizedTranscript and TestAudioWebSocketHandler_TranscriptAtMaxLength added. All backend tests pass.", "done_when": "processTranscript in websocket_interaction.go rejects transcripts exceeding 500 chars (consistent with HTTP /api/intent). Test added. All backend tests pass."}
{"id": 285, "description": "Add focus trap to FeedbackButton modal", "status": "done", "done_when": "Tab/Shift+Tab wraps focus within modal content. Keydown handler traps focus to focusable elements. E2E test 'focus stays trapped within modal on Tab' verifies wrap-around. 56 E2E tests (5 feedback) pass. Build passes.", "done_when": "Tab key cycles within modal elements (close button, form fields, submit button). Shift+Tab wraps backward. Focus returns to feedback button on close. E2E test verifies focus trapping."}
{"id": 286, "description": "Restore focus to setup button when TOTP setup is canceled on settings page", "status": "done", "done_when": "Cancel setup handler calls setupTotpBtn.focus(). Cancel disable handler calls disableTotpBtn.focus(). Build passes. 11 settings E2E tests pass.", "done_when": "Cancel button click handler calls setupTotpBtn.focus(). Cancel disable flow handler calls disableTotpBtn.focus(). Build passes."}
{"id": 287, "description": "Clear reconnect timer at start of WebSocket connect() to prevent double-connect", "status": "done", "done_when": "connect() calls clearReconnectTimer() before creating new WebSocket. 'cancels pending reconnect on manual connect' test added. 325 unit tests pass.", "done_when": "connect() calls clearReconnectTimer() before creating new WebSocket. Unit test verifies reconnect timer cleared on manual connect. All frontend tests pass."}
{"id": 288, "description": "Deep inspection round 5: health check context propagation, bcrypt docs fix, WHISPER_SERVER_URL default docs", "status": "done", "done_when": "health.go checkWhisperServer/checkPiperServer accept context.Context, callers pass r.Context(). SECURITY.md says 'bcrypt with cost 12'. AGENTS.md WHISPER_SERVER_URL default is 'http://127.0.0.1:8765'. All backend tests + lint pass."}
{"id": 289, "description": "Add HandleResendVerification tests for rate limiting and edge cases", "status": "done", "done_when": "Tests cover: successful resend, already-verified email, rate limit (5 in 24h), email send failure handling. All backend tests pass."}
{"id": 290, "description": "Add email template rendering tests to verify HTML escaping", "status": "done", "done_when": "Tests verify VerificationHTML/Text and MagicLinkHTML/Text render correctly. Tests verify URLs with special characters are properly escaped. All backend tests pass."}
{"id": 291, "description": "Add periodic cleanup for expired sessions and login attempts", "status": "done", "done_when": "main.go starts a background goroutine that periodically calls DeleteExpiredSessions and DeleteExpiredLoginAttempts (e.g. every hour). Goroutine stops on cleanupCtx cancellation. Test verifies cleanup runs. All backend tests pass."}
{"id": 292, "description": "Add oversized body tests for auth endpoints (register, login, magic-link, resend-verification)", "status": "done", "done_when": "Tests send 5KB+ bodies to register, login, magic-link, resend-verification and verify 413 response. Pattern matches existing TestSpeakHandler_BodyTooLarge. All backend tests pass."}
{"id": 293, "description": "Add try-catch around response.json() calls in frontend text-to-speech.ts and settings page", "status": "done", "done_when": "text-to-speech.ts wraps response.json() in try-catch with fallback error. Settings page TOTP/user-info JSON parsing has try-catch. Frontend build passes. No new test failures."}
{"id": 294, "description": "Add explicit 413 handling to TOTP enable/disable handlers for oversized bodies", "status": "done", "done_when": "HandleTOTPEnable and HandleTOTPDisable check for 'http: request body too large' error before generic 'invalid JSON body'. Tests verify 413 response for 5KB body. All backend tests pass."}
{"id": 295, "description": "Add expired verification and magic link token cleanup to auth cleanup goroutine", "status": "done", "done_when": "CleanupExpiredAuth also deletes expired (past expires_at) verification and magic link tokens. DB methods added if needed. Tests verify token cleanup. All backend tests pass."}
{"id": 296, "description": "Clean up empty IP entries from WSAuthTracker.anonInteractions map", "status": "done", "done_when": "CleanupStaleEntries method added to WSAuthTracker — removes IPs with no recent interactions. Test verifies stale entries removed while fresh entries preserved. All backend tests pass."}
{"id": 297, "description": "Add LIMIT clause to ListExpiredAudioBlobs query to prevent memory spikes", "status": "done", "done_when": "ListExpiredAudioBlobs uses audioBlobBatchSize (500) LIMIT. CleanupExpiredAudioBlobs loops until empty batch returned. All backend tests pass."}
{"id": 298, "description": "Add max buffer size check to WebMParser.Append to prevent unbounded memory growth", "status": "done", "done_when": "WebMParser.Append returns bool (false on overflow). defaultMaxBufferSize=50MB. Both audio handlers drop chunks on overflow with warning log. Test verifies buffer size enforcement. All backend tests pass."}
{"id": 299, "description": "Add Access-Control-Allow-Credentials header to CORS middleware for cookie-based auth", "status": "done", "done_when": "CORSMiddleware sets Access-Control-Allow-Credentials: true when allowedOrigin is not '*'. CORS test updated. All backend tests pass."}
{"id": 300, "description": "Drain LLM HealthCheck response bodies on success path for connection reuse", "status": "done", "done_when": "Anthropic and OpenAI HealthCheck methods drain resp.Body with io.LimitReader on 200 success path (not just error path). All backend tests pass."}
{"id": 301, "description": "Guard TOTP setup button against double-click race condition", "status": "done", "done_when": "Verified: setupTotpBtn.disabled=true runs synchronously at line 357 before any await. Browser won't fire click events on disabled buttons. No additional guard needed — existing code is correct."}
{"id": 302, "description": "Wire WSAuthTracker into production WebSocket handler in main.go", "status": "done", "done_when": "main.go creates NewWSAuthTracker(DefaultWSAuthLimits()) and sets wsHandler.AuthTracker. Per-user and per-IP connection/interaction limits enforced in production. All backend tests pass."}
{"id": 303, "description": "Fix settings page TOTP setup errors invisible when parent flow container is hidden", "status": "done", "done_when": "Moved setup-error and disable-error divs outside their flow containers (same pattern as setup-success). Errors now visible even when flow section hasn't been revealed. Build passes, 11 E2E settings tests pass."}
{"id": 304, "description": "CSRF middleware should fail-closed on database errors instead of passing requests through", "status": "done", "done_when": "CSRFMiddleware returns 500 with 'internal error' when GetSessionByToken fails, instead of passing request through. TestCSRFMiddleware_DBError_Returns500 verifies behavior. All backend tests pass."}
{"id": 305, "description": "Fix AGENTS.md sops command to convert YAML to shell format", "status": "done", "done_when": "AGENTS.md sops command now uses grep+sed pipeline to convert YAML to shell format, matching DEPLOY.md."}
{"id": 306, "description": "Add error logging to encrypted_media.go serveEncrypted for file open and decryption failures", "status": "done", "done_when": "serveEncrypted now logs slog.Error with path and error details for both file open and decryption failures. All encrypted media tests pass."}
{"id": 307, "description": "Add missing database indexes on token_hash columns for email_verification_tokens and magic_link_tokens", "status": "done", "done_when": "Added idx_email_verification_token_hash and idx_magic_link_token_hash indexes. These columns are queried on user-facing auth paths (email verification, magic link login) but had no index, causing full-table scans. All backend tests pass."}
{"id": 308, "description": "Wire LLMProviderName into WebSocket handler in main.go for interaction logging", "status": "done", "done_when": "wsHandler.LLMProviderName set from LLM_PROVIDER env var (default 'anthropic'). Previously empty string, causing interaction logs to have missing provider name. Build passes."}
{"id": 309, "description": "Split oversized test files to stay under 500-line convention", "status": "done", "done_when": "Split 3 oversized test files into 6 files: websocket_validation_test.go (592→356+251), websocket_framing_test.go (541→265+255), ratelimit_test.go (514→326+197). All under 500 lines, all tests pass."}
{"id": 310, "description": "Add rate limiting to logout and log endpoints", "status": "done", "done_when": "POST /api/auth/logout now rate-limited at 10/min, POST /api/log at 30/min. Updated API.md rate limit table. Build passes."}
{"id": 311, "description": "Warn on unrecognized LOG_LEVEL value instead of silently falling back to info", "status": "done", "done_when": "parseLevel now writes stderr warning for unrecognized LOG_LEVEL values. Explicit 'info' and empty string handled as separate valid cases. Existing tests pass."}
{"id": 312, "description": "Fix isProcessing race in stop_recording path — set flag before spawning processAudio", "status": "done", "done_when": "stop_recording handler now sets state.isProcessing=true before spawning processAudio goroutine, matching bufferThresholdWatcher behavior. Prevents concurrent processAudio from buffer watcher when client quickly restarts recording, and prevents idle timeout from closing connection mid-processing. All tests pass."}
{"id": 313, "description": "Add no-referrer meta tag to verify-email and magic-link pages to prevent token leakage via Referer header", "status": "done", "done_when": "Both pages now have <meta name=\"referrer\" content=\"no-referrer\"> in <head>. Defense-in-depth layered on top of backend Referrer-Policy header. Frontend build passes."}
{"id": 314, "description": "Add rate limiting to admin feedback endpoint (20/min)", "status": "done", "done_when": "GET /api/admin/feedback now rate-limited at 20/min per IP. Updated API.md rate limit table. Build passes. This was the last endpoint without rate limiting."}
{"id": 315, "description": "Hash session tokens before database storage (defense-in-depth against DB compromise)", "status": "done", "done_when": "Session tokens stored as SHA-256 hash in database (like email verification and magic link tokens). Schema updated, migration tested, all session lookup/create/delete paths updated. Auth tests pass."}
{"id": 316, "description": "Add startup warning when MEDIA_DIR contains .age files but no age key is loadable", "status": "done", "done_when": "On startup, if seedMediaFromDisk finds .age files and EncryptedFileServer is NOT being used (age key missing/unloadable), log slog.Error with a clear message like 'encrypted media files found but age key not available — media will be inaccessible'. This prevents silent fallback to plain file server that 404s on all media."}
{"id": 317, "description": "Add exponential backoff to buffer threshold watcher on repeated empty transcripts", "status": "done", "done_when": "After N consecutive empty transcripts (e.g., 3), the buffer threshold watcher increases the check interval (e.g., double it up to a max of 10s). Resets to normal threshold on first non-empty transcript. Prevents hammering whisper with the same audio data and exhausting rate limits. Backend tests pass."}
{"id": 318, "description": "Add connection timeout to WebSocket connect() in websocket-audio.ts", "status": "done", "done_when": "AudioWebSocket.connect() has a 10-second timeout. If neither onopen nor onclose fires within 10s, the promise rejects with a timeout error and the socket is closed. Prevents indefinite hangs on networks that silently drop WebSocket upgrades. Timeout is cleared on open/close. Frontend unit tests pass."}
{"id": 319, "description": "Separate JSON parse errors from handler errors in WebSocket handleMessage", "status": "done", "done_when": "In websocket-audio.ts handleMessage(), JSON.parse is wrapped in its own try-catch (logs 'invalid JSON'). A separate try-catch wraps the switch/case dispatch (logs 'error handling WebSocket message'). This prevents handler exceptions from being misreported as JSON parse errors. Frontend tests pass."}
{"id": 320, "description": "Add debug logging when WebM TrimBefore returns 0 bytes trimmed", "status": "done", "done_when": "In websocket_audio.go processAudio(), when TrimBefore returns 0 (no cluster boundary found), a logger.Debug is emitted with buffer_size and trim_time_ms before the fallback Clear(). Aids troubleshooting buffer trimming issues in production. Backend tests pass."}
{"id": 321, "description": "Schedule periodic WSAuthTracker.CleanupStaleEntries in main.go", "status": "done", "done_when": "main.go starts a background goroutine that calls wsHandler.AuthTracker.CleanupStaleEntries() every 10-15 minutes, similar to existing auth and interaction cleanup goroutines. Goroutine stops on context cancellation. Prevents unbounded growth of anonInteractions map from one-off anonymous IPs. Backend tests pass."}
{"id": 322, "description": "Add length validation to extractSessionToken", "status": "done", "done_when": "extractSessionToken returns empty string if token exceeds maxTokenLength (128 chars). Consistent with verify/magic-link token length validation. Prevents arbitrarily large tokens from being SHA-256 hashed. Backend tests pass."}
{"id": 323, "description": "Cache checkAuth() result to prevent triple API calls on page load", "status": "done", "done_when": "auth.ts checkAuth() caches the in-flight promise so multiple concurrent callers (LoginButton, LogoutButton, SettingsButton) share one /api/auth/me request instead of three. Add resetAuthCache() for logout. Frontend build passes, tests pass."}
{"id": 324, "description": "Clear password input on successful TOTP enable in settings page", "status": "done", "done_when": "After successful TOTP verification in settings.astro, clear setupPasswordInput.value and totpCodeInput.value (matching the cancel flow behavior). Frontend build passes."}
{"id": 325, "description": "Add double-tap guard to PeekabooFlow.startRecording()", "status": "done", "done_when": "startRecording() sets state to 'recording' (or an isStarting flag) before awaiting getUserMedia/WebSocket connect. Prevents duplicate microphone streams from rapid double-tap. Frontend tests pass."}
{"id": 326, "description": "Fix go.mod direct/indirect dependency classification", "status": "done", "done_when": "go.mod correctly lists filippo.io/age, github.com/at-wat/ebml-go, github.com/coder/websocket, github.com/mattn/go-sqlite3, github.com/resend/resend-go/v3, and golang.org/x/crypto as direct dependencies (without // indirect comment). go build and go test pass."}
{"id": 327, "description": "Harden systemd service file with security directives", "status": "done", "done_when": "docs/peekaboo.service includes NoNewPrivileges=yes, ProtectSystem=strict, ReadWritePaths for data dir, PrivateTmp=yes, TimeoutStopSec=30. DEPLOY.md notes updated if needed. All directives are compatible with user-level systemd service."}
{"id": 328, "description": "Add .catch() to unhandled sendAudioChunk promise in peekaboo-flow.ts ondataavailable", "status": "done", "done_when": "ondataavailable handler wraps sendAudioChunk() call with .catch() that logs at debug level. Frontend unit tests pass. Build passes."}
{"id": 329, "description": "Add aria-hidden=true to decorative SVG icons in Astro components", "status": "done", "done_when": "All decorative SVGs in LoginButton, LogoutButton, SettingsButton, ThemeToggle, FeedbackButton, MicButton have aria-hidden=true. Back-button SVGs on login/register pages also have aria-hidden=true. Build passes, E2E tests pass."}
{"id": 330, "description": "Add early session token hex format validation in CSRF middleware before DB lookup", "status": "done", "done_when": "CSRF middleware validates session token is valid hex and exactly 64 chars before calling HashToken+GetSessionByTokenHash. Invalid format tokens skip CSRF check (passed to handler for auth rejection). Backend tests pass."}
{"id": 331, "description": "Update specs/auth.md to match current code: fix handler file path and token_hash column name", "status": "done", "done_when": "specs/auth.md line 30 references backend/api/handlers_auth.go (not backend/handlers_auth.go). Sessions table schema uses token_hash column (not token) and idx_sessions_token_hash index name (not idx_sessions_token). These match the actual code in backend/db/db_auth.go."}
{"id": 332, "description": "Fix star rating accessibility: replace radiogroup with aria-pressed button pattern", "status": "done", "done_when": "FeedbackButton.astro star rating container no longer uses role=radiogroup. Each star button uses aria-pressed (true when selected, false otherwise). aria-labelledby on container changed to aria-label or removed. updateStars() sets aria-pressed. E2E feedback tests pass, build passes."}
{"id": 333, "description": "PBT RED: Write pbt.config.ts and test spec files that define the PBT API", "status": "todo", "done_when": "frontend/tests/pbt/pbt.config.ts exists (workers:1, timeout:120000, no webServer). Test files exist: show-cat.spec.ts, show-unknown.spec.ts, two-commands.spec.ts, fixture-cat.spec.ts. Each imports createPBT from ../helpers/pbt/runner and uses the API from specs/pbt.md (say, sayFixture, assertMedia, assertNoMedia, assertTTS, assertNoTTS, assertTranscript, clear). Tests will NOT compile yet — that's expected. Also add test:pbt script to frontend/package.json."}
{"id": 334, "description": "PBT GREEN: Write types.ts, piper-client.ts, and audio-pipeline.ts", "status": "todo", "done_when": "frontend/tests/helpers/pbt/types.ts defines PBTConfig and SynthesizedAudio. piper-client.ts exports synthesizeWAV(text, piperUrl) that POSTs JSON {text, length_scale} to Piper and returns WAV Buffer. audio-pipeline.ts exports wavToWebmOpus (ffmpeg), splitIntoChunks (4KB base64), synthesizeAndChunk (full pipeline), and fixtureToChunks (read .webm fixture). Verify piper-client works: source .env, call Piper at PIPER_SERVER_URL with a test phrase, confirm WAV bytes returned. Verify audio-pipeline works: convert the WAV to WebM, confirm non-empty chunks array."}
{"id": 335, "description": "PBT GREEN: Write scripted-recorder.ts and ws-observer.ts", "status": "todo", "done_when": "frontend/tests/helpers/pbt/scripted-recorder.ts exports getScriptedRecorderScript() returning a function for page.addInitScript(). ScriptedMediaRecorder reads sessions from window.__PBT_SESSIONS__, advances session index on each start() call, emits base64 chunks at timeslice intervals, flushes on stop(). Includes mock getUserMedia. frontend/tests/helpers/pbt/ws-observer.ts exports getWSObserverScript() that wraps WebSocket to capture incoming JSON messages into window.__PBT_WS_MESSAGES__ and exposes window.__PBT_CLEAR_MESSAGES__()."}
{"id": 336, "description": "PBT GREEN: Write runner.ts — createPBT factory and PBTRunner class", "status": "todo", "done_when": "frontend/tests/helpers/pbt/runner.ts exports createPBT(page, opts?). PBTRunner implements: say(text) — synthesize via Piper+ffmpeg, inject recorder on first call via addInitScript or push session via page.evaluate on subsequent calls, navigate on first call, click mic. sayFixture(filename) — same but reads from tests/fixtures/. assertMedia(subject), assertNoMedia, assertTTS, assertNoTTS, assertTranscript, assertError — all use Playwright expect() with configurable timeout. clear() — resets WS message buffer. All test spec files from task 333 now compile (npx tsc --noEmit or playwright test --list)."}
{"id": 337, "description": "PBT GREEN: Get fixture-cat.spec.ts passing with real services", "status": "todo", "done_when": "source .env && cd frontend && npx playwright test --config tests/pbt/pbt.config.ts fixture-cat passes. Backend, frontend dev server, and Whisper must be running. The test uses sayFixture('me-show-me-a-cat.webm') so Piper is NOT required. Fix any bugs found during this first real run."}
{"id": 338, "description": "PBT GREEN: Get show-cat.spec.ts passing with real services (requires Piper)", "status": "todo", "done_when": "source .env && cd frontend && npx playwright test --config tests/pbt/pbt.config.ts show-cat passes. This exercises the full pipeline: Piper TTS synthesis -> ffmpeg -> ScriptedMediaRecorder -> WebSocket -> Whisper -> LLM -> media response. Backend, frontend, Whisper, and Piper must all be running. Fix any bugs in the Piper/audio pipeline."}
{"id": 339, "description": "PBT GREEN: Get remaining specs passing (show-unknown, two-commands)", "status": "todo", "done_when": "source .env && cd frontend && npm run test:pbt passes — all 4 spec files pass. show-unknown.spec.ts verifies TTS fallback for unknown subjects. two-commands.spec.ts verifies multi-say with clear() between commands. Fix any bugs in multi-session recorder or WS observer."}
{"id": 340, "description": "PBT REFACTOR: Clean up helpers for readability", "status": "todo", "done_when": "All PBT helper files in frontend/tests/helpers/pbt/ are under 150 lines each. runner.ts has clear separation between setup, say, and assertion logic. No duplicated code between helpers. Type exports are clean. All tests still pass (npm run test:pbt). Existing e2e tests still pass (npx playwright test). lint.sh passes."}
{"id": 341, "description": "PBT DOCS: Update specs/pbt.md and AGENTS.md with final implementation details", "status": "todo", "done_when": "specs/pbt.md reflects the actual implemented API (fix any drift from spec to implementation). Add test:pbt to the Commands section in AGENTS.md. Environment Setup section in specs/pbt.md has accurate service URLs. All file paths in the spec match reality. No stale references."}
