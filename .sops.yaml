# sops configuration for encrypting secrets
# Replace the age public key below with your own after running:
#   age-keygen -o ~/.config/sops/age/keys.txt
# The public key will be displayed as: age1abc123...

creation_rules:
  - path_regex: \.enc\.yaml$
    age: age1dlv4emz589e2r7fyrudstaxw787as9dcpv0a93hg6d2n9p7tdexsvn5w7v
